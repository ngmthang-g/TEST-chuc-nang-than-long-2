name: Build Than Long Windows Setup

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  hold:
    runs-on: windows-latest
    steps:
      - run: Write-Host "Setup source upload in progress"
